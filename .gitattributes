# Ensure Python and shell scripts are treated as text files, not executables
*.py text
*.sh text
*.bash text

# Web assets
*.html text
*.css text
*.js text
*.json text
*.md text

# Ensure these files are never treated as executable
bin/*.py -x
scripts/*.py -x
scripts/**/*.sh -x
*.py -x
*.sh -x 