# Minimal requirements for RAD Monitor dashboard generation
# Used by GitHub Actions for deployment

# Core dependencies only
requests>=2.31.0
python-dateutil>=2.8.2
pydantic>=2.5.0
pydantic-settings>=2.0.0 