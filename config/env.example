# RAD Monitor Configuration
# Copy this file to .env and fill in your values

# ===== Elasticsearch Settings =====
# Authentication cookie for Elasticsearch (required)
ES_COOKIE=your_elasticsearch_cookie_here

# Elasticsearch URL (optional, has default)
# ES_URL=https://usieventho-prod-usw2.es.us-west-2.aws.found.io:9243

# Index pattern for queries (optional, default: traffic-*)
# ES_INDEX_PATTERN=traffic-*

# Query timeout in seconds (optional, default: 30)
# ES_TIMEOUT=30

# ===== Kibana Settings =====
# Kibana base URL (optional, has default)
# KIBANA_URL=https://usieventho-prod-usw2.kb.us-west-2.aws.found.io:9243

# Path to Kibana discover (optional, default: /app/discover#/)
# KIBANA_DISCOVER_PATH=/app/discover#/

# ===== Processing Settings =====
# Baseline period start date in ISO format (required)
# Example: 2024-01-01T00:00:00
BASELINE_START=2024-01-01T00:00:00

# Baseline period end date in ISO format (required)
# Example: 2024-01-07T00:00:00
BASELINE_END=2024-01-07T00:00:00

# Current period time range (optional, default: now-12h)
# Examples: now-12h, now-24h, now-7d, inspection_time
# CURRENT_TIME_RANGE=now-12h

# High volume event threshold (optional, default: 1000)
# HIGH_VOLUME_THRESHOLD=1000

# Medium volume event threshold (optional, default: 100)
# MEDIUM_VOLUME_THRESHOLD=100

# Critical score threshold (optional, default: -80)
# CRITICAL_THRESHOLD=-80

# Warning score threshold (optional, default: -50)
# WARNING_THRESHOLD=-50

# ===== Dashboard Settings =====
# Auto-refresh interval in seconds (optional, default: 300)
# DASHBOARD_REFRESH_INTERVAL=300

# Maximum events to display (optional, default: 200)
# DASHBOARD_MAX_EVENTS_DISPLAY=200

# Enable real-time updates via WebSocket (optional, default: false)
# DASHBOARD_ENABLE_WEBSOCKET=false

# UI theme - light, dark, or auto (optional, default: light)
# DASHBOARD_THEME=light

# ===== Application Settings =====
# Application name (optional, default: RAD Monitor)
# APP_NAME=RAD Monitor

# Debug mode (optional, default: false)
# DEBUG=false

# Allowed CORS origins (optional, comma-separated list)
# Default: http://localhost:3000,http://localhost:8000
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000,https://yourdomain.com
